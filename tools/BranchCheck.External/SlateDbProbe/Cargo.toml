[package]
name = "branchcheck-slatedb-probe"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
slatedb = "=0.14.1"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }

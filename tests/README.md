# Tests

Tests are separated by failure model rather than by production project alone. Pure logic, real persistence, reference-model correctness, and process-crash recovery require different fixtures and evidence. See `ARCHITECTURE.md` for ownership.

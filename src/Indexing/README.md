# Indexing

The abstraction project defines the stable collision-safe logical index seam. Baseline is the managed reference implementation. Future optimized implementations must implement the same contract and remain selectable only by the `ChronicleDB` composition root.
